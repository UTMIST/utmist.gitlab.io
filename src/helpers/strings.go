package helpers

// PrintDateTimeLayout defines the layout we print out.
const PrintDateTimeLayout = "Monday, January 2, 2006, 3:04PM"

// PrintDateLayout defines the layout we print out.
const PrintDateLayout = "Monday, January 2, 2006"

const parseDateLayout = "2006-01-02"
const parseDateTimeLayout = "2006-01-02 15:04"

// ContentDirectory is the path for where content files are stored.
const ContentDirectory = "content/"

// MarkdownExt is the extension for markdown files.
const MarkdownExt = ".md"

// PageIndex is the filename of a page's index.
const PageIndex = "index.md"

// GroupIndex is the filename of a page group's index.
const GroupIndex = "_index.md"
